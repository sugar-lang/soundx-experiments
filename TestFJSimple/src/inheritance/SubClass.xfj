package inheritance;

import basics.FJInteger;
import inheritance.SuperClass;

public class SubClass extends SuperClass<FJInteger> {
	public void inc(int delta) {
		FJInteger fld = getFld();
		int old = fld.getVal();
		fld.setVal(delta + old);
	}
}